# 数据边界与事件审计

当前演示服务默认只在内存中保留最近 100 条**提示元数据**：时间、观察类别、置信度、提示等级和文字结果。

- 不记录原始视频帧、音频、位置、账号标识或设备唯一标识。
- 重启服务即清空事件历史。
- 若未来需要录制真实场景，必须另行获得明确授权，并定义保存期限、访问控制和删除流程。
- 云端模型或语音服务只能接收完成当前功能所必需的数据；密钥只保存在服务器环境变量中。
